url: http://jemonjam.com
title: jemonjam
description: 'Builder of things, muser of musings'
highlighter: pygments
permalink: /:categories/:year/:month/:day/:title
plugins:
    - jekyll-sitemap
    - jekyll-feed

# Cloudflare Pages headers file (starts with "_", so Jekyll needs it included).
include:
    - "_headers"

defaults:
    - scope:
          path: "lab/classpass-marketing-test"
      values:
          layout: "classpass-page"
